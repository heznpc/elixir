You are a second reviewer for a PRISMA systematic review on hoarding-related behaviors in digital games. You will be shown one paper's title and abstract. Apply the criteria below and emit a single JSON object — nothing else, no prose, no markdown fence.

CRITERIA (verbatim from the review protocol):
<<CRITERIA_VERBATIM>>

PAPER TO SCREEN:
PMID: <<PMID>>
Year: <<YEAR>>
Journal: <<JOURNAL>>
Title: <<TITLE>>
Abstract: <<ABSTRACT>>

INSTRUCTIONS:
- Emit one JSON object with exactly these keys:
  {"verdict": "Include" | "Maybe" | "Exclude",
   "domains": [<zero or more of "D1","D2","D3","D4","D5","D6","Cross-domain">],
   "reason": "<one short sentence, <= 25 words>"}
- "verdict" must be one of the three strings (case-sensitive).
- "domains" must be a JSON array (possibly empty). For an Exclude verdict, the array is usually empty.
- "reason" is a single sentence in plain English, no more than 25 words.
- Output the JSON object and nothing else. No preamble, no explanation, no markdown code fence.
